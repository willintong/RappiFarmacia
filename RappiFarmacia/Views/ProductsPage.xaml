<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RappiFarmacia.Views.ProductsPage">

    <ListView x:Name="Listaproductos" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Image HeightRequest="50" WidthRequest="50" Source="{Binding Foto}"></Image>
                        <Label Text="{Binding Nombre}"></Label>
                        <Label Text="{Binding Valor }"></Label>
                    
                    <StackLayout Orientation="Horizontal">
                    <Button Text="Volver al perfil" Clicked="Perfil_Click"/>
                    </StackLayout>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>