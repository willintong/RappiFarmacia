<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RappiFarmacia.Views.ProductsPage">

    <ListView x:Name="Listaproductos" HasUnevenRows="True">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Image HeightRequest="50" WidthRequest="50" Source="{Binding image}"></Image>

                        <Label Text="{Binding code}" Grid.Column="1" Grid.Row="0" />
                        <Label Text="{Binding name}" Grid.Column="1" Grid.Row="1" />
                        <Label Text="{Binding salePrice}" Grid.Column="1" Grid.Row="2" />
                        <Label Text="{Binding purchasePrice}" Grid.Column="1" Grid.Row="3" />
                        <Label Text="{Binding stock}" Grid.Column="1" Grid.Row="4" />


                        <StackLayout Orientation="Horizontal">
                    <Button Text="Volver al perfil" Clicked="Perfil_Click"/>
                    </StackLayout>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>